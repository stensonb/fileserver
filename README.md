# fileserver

A simple server to easily exchange files on your local network.

To see all options, try:
```
./fileserver -h
```

# Development
## releasing a new version
```
$ git tag vX.X.X -m "release message here"
$ ./scripts/release.sh
```

## todo
* use chi/docgen to document all valid calls?
* tests
